import React from 'react';

const WebhookPayload = () => {
  React.useEffect(() => {
    // eslint-disable-next-line no-console
    console.warn('Placeholder: WebhookPayload is not implemented yet.');
  }, []);
  return <>{/*WebhookPayload */}</>;
};

export { WebhookPayload };
